my-portfolio/
│── public/                 # Static assets (profile image, favicon, etc.)
│── src/
│   ├── app/                # App router (Next.js 13+)
│   │   ├── layout.tsx      # Root layout
│   │   ├── page.tsx        # Home page
│   │   └── about/page.tsx  # About page (example)
│   │
│   ├── components/         # Reusable UI components
│   │   ├── Navbar.tsx
│   │   └── Footer.tsx
│   │
│   ├── styles/             # Global styles
│   │   └── globals.css
│   │
│   ├── lib/                # Helpers / utils (for later)
│   ├── types/              # TypeScript types (for later)
│   └── config/             # App constants/configs (for later)
│
│── learn.txt               # Your personal learning notes
│── package.json
│── tsconfig.json
│── next.config.js
│── postcss.config.js
│── tailwind.config.js      # If you choose Tailwind


starte management

src/
 ├── features/
 │    ├── auth/
 │    │    ├── store.ts   # Zustand (client state)
 │    │    ├── api.ts     # React Query hooks (server state)
 │    │    └── types.ts
 │    └── projects/
 │         ├── store.ts
 │         ├── api.ts
 │         └── types.ts
 auth projects nav and all that

  <Image
                        width={900}
                        height={600}
                        className="object-contain"
                        src={project.image} alt=''
                        />


                    product=useParams()
                <Link href={`/products/${product.id}`}>
              {product.name} - ${product.price}
            </Link>
next
  const [projectObjDetails,setProjectObjDetails]=useState<projectsdetail>({} as projectsdetail)
  
  next
  React components must start with an uppercase letter.

✅ AllProjectsPage

❌ page

Hooks can only be called inside function components or custom hooks.

In the Next.js App Router, the file name page.tsx doesn’t matter — Next.js will pick it up anyway. What matters is the function’s name inside.
